# Local development: the runtime dependencies (mirrored in the root
# requirements.txt, which is what Vercel installs) plus test/lint tooling.
#
# Removed from the original scaffold:
#   emergentintegrations==0.1.0  - not published on PyPI; breaks any clean install
#   boto3, pandas, numpy, requests, requests-oauthlib, python-jose, jq, typer,
#   python-multipart, cryptography, tzdata - imported nowhere in this codebase
#
# passlib was dropped deliberately: passlib 1.7.x reads
# bcrypt.__about__.__version__, which bcrypt 4.1+ removed. app/security.py calls
# the bcrypt API directly instead.
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.5.0
pydantic>=2.6.4
email-validator>=2.2.0
python-dotenv>=1.0.1
PyJWT>=2.10.1
bcrypt==4.1.3
httpx>=0.27.0

# Tooling
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.1.1
isort>=5.13.2
flake8>=7.0.0
mypy>=1.8.0
mongomock_motor>=0.0.21  # dev-only: in-process MongoDB stand-in (USE_MEMORY_DB)
