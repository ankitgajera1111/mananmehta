# Runtime dependencies for the Vercel Python function (api/index.py).
# Kept deliberately small: every package here is unzipped into the Lambda on
# each cold start, so anything unused directly costs response time.
#
# Notably absent, and on purpose:
#   emergentintegrations - not published on PyPI; it would fail the build
#   pandas / numpy / boto3 - scaffolding leftovers, imported nowhere
#   cloudinary / resend SDKs - both APIs are a single signed HTTP call, which
#                              httpx already covers
fastapi==0.110.1
motor==3.3.1
pymongo==4.5.0
pydantic>=2.6.4
email-validator>=2.2.0
python-dotenv>=1.0.1
PyJWT>=2.10.1
bcrypt==4.1.3
httpx>=0.27.0
